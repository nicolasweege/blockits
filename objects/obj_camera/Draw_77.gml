
/*
gpu_set_texfilter(false);

var screen_scale = (window_get_width() / global.cam_width);

draw_surface_ext(application_surface, 
                 (0 - (frac(global.camx) * screen_scale)), 
				 (0 - (frac(global.camy) * screen_scale)), 
				 screen_scale, 
				 screen_scale,
				 0,
				 c_white,
				 1.0);
				 
gpu_set_texfilter(true);
*/